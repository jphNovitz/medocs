{% extends 'base.html.twig' %}
{% block body %}
    <section class="bg-surface-light dark:bg-surface-dark">
        <div class="py-8 px-4 mx-auto max-w-2xl lg:py-16">
            <twig:title:h2>{% trans %}title.product.new {% endtrans %}</twig:title:h2>
            {{ include('member/product/_form.html.twig', {'button_label': 'button.add'}) }}
        </div>
    </section>

{% endblock %}
