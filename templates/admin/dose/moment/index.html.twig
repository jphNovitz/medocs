{% extends 'base_back.html.twig' %}

{% block title %}Moments List{% endblock %}

{% block body %}

    <a href="{{ path('admin_dose_moment_new') }}">Nouveau</a>

    {# list if exist #}
    {% if list is defined and list is not empty %}
        {% for item in list %}
            <p>{{ item.name }} | modifier | supprimer</p>
        {% endfor %}
    {% else %}
        <p>Aucun élément</p>
    {% endif %}
{% endblock %}
